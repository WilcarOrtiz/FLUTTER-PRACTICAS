import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF187BFC); //  Color 4: Azul fuerte
  static const primaryAccent = Color(0xFF1F87FD); //  Color 3: Azul medio
  static const primaryLight = Color(0xFF4EC9F4); //  Color 1: Celeste claro
  static const secondary = Color(0xFF59DFAA); //  Color 2: Verde menta
  static const background = Color(
    0xFFF7FAFF,
  ); //  Color 5: Fondo general (no blanco)

  /// Texto y otros
  static const textPrimary = Color(0xFF212121); // Gris oscuro casi negro
  static const textSecondary = Color(0xFF757575); // Gris medio

  /// Colores adicionales útiles
  static const white = Colors.white;
  static const black = Colors.black;
  static const success = Color(0xFF43A047);
  static const error = Color(0xFFE53935);
  static const warming = Color.fromARGB(255, 236, 158, 40);
}
